Password Management Software:
The password manager can be run from the main class `LoginWindow.py` found in `src/gui`.

Required packages for this program can be found in the requirements.txt file.
The program can be run with `pip install -r requirements.txt` and `python src/gui/LoginWindo.py`
